# Agent Rules & Instructions

## Build & Verification Guidelines
- **Do NOT run build commands** (such as `npm run build`, `next build`, `npx tsc --noEmit`, etc.) automatically after code edits unless explicitly requested by the user.
- Focus on direct, accurate code modifications without invoking full project builds.
